type PageInfo {
    cursor: String!
    hasMore: Boolean!
}

interface Connection {
    pageInfo: PageInfo!
    edges: [Edge]!
}

interface Edge {
    cursor: String!
    node: Node!
}

interface Node {
    id: ID!
}


input PaginationInput {
    cursor: String!
}
